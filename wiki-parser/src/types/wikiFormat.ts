// defines the csv-headers expected by the wiki parser
export type WikiFormat = {
    Name: string;
    Land: string;
    Form: string;
    Träger: string;
    Promotionsrecht: string;
    Studierende: string;
    Stand: string;
}